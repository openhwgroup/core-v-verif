// #include <stdio.h>
// #include <stdlib.h>

#include "pmp.h"

int main(int argc, char *argv[])
{
  // reset_registers();
  // default_full();
  // default_none();
  // mmode_only();
  // napot_matching();
  tor_macthing();

  exit(EXIT_SUCCESS);
}
